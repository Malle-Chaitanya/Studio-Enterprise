node_modules/
dist/
build/
.env
.env.local
*.log

# Never commit credentials
*sa-key*.json
*service-account*.json
service_account.json
secrets/
# SSH deploy keys — the private half lives only in the GitHub secret DEPLOY_SSH_KEY
*_deploy
*_deploy.pub
id_ed25519*
id_rsa*
*.pem
migration_data/

# Editor/build junk
~$*
*.tsbuildinfo
__pycache__/
*.pyc

# Claude Code — personal overrides & local settings (never commit)
CLAUDE.local.md
.claude/settings.local.json
.claude/launch.json

# Local reference dumps & local-only test artifacts (not app code)
discovery_v1alpha.json
server/adk_local_test/
*.docx
.gstack/

# Editor workspace settings (personal)
.vscode/

# Unrelated nested reference repo (has its own .git)
content-trunk/
